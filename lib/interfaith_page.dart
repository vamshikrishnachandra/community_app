import 'package:flutter/material.dart';

class InterfaithPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Interfaith Networking Page"),
    );
  }
}
